/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', // Usar clase 'dark' en el elemento <html>
  theme: {
    extend: {
      colors: {
        // Extensiones de colores si es necesario
      },
    },
  },
  plugins: [],
}
